REMOVED: Temporarily removed so the README can be re-introduced via a pull request.

This change intentionally clears the README content on main so the branch `docs/add-octoacme-readme-issue-2` can be used to create a pull request that adds the canonical README.md file and closes issue #2.

If you want me to instead delete the file, create a PR, or take a different approach, tell me and I will proceed.
